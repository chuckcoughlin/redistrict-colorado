<!-- Build and deploy the shapefile viewer -->
<project name="viewer.install" default="all" basedir=".">
	<import file="${basedir}/build.xml"/>
	<property name="jar.name" value="shapefile-viewer.jar" />
	<property name="src.dir" value="${basedir}/../ShapeViewer/src" />
	
	<!-- This target will build the core application and deploy it to the robot -->
	<target name="all" depends="clean, build, deploy" />
	
	<!-- Remove the existing target -->
	<target name="clean" description="Remove existing build products">
		<delete dir="${temp.dir}"/>
		<mkdir dir="${temp.dir}"/>
		<mkdir dir="${lib.dir}"/>
		<mkdir dir="${bin.dir}"/>
		<delete file="${lib.dir}/${jump-core-jar}" />
		<delete file="${lib.dir}/${jump-ui-jar}" />
		<delete file="${lib.dir}/${shapefile-viewer-jar}" />
		<delete dir="${temp.dir}" />
		<delete file="${install.dir}/lib/${jump-core-jar}" />
		<delete file="${install.dir}/lib/${jump-ui-jar}" />
		<delete file="${install.dir}/lib/${shapefile-viewer-jar}" />
	</target>
	
	<target name="build" depends="jump-core,jump-ui,shape-viewer" />
	<target name="jump-core" description="Build OpenJUMP core jar">
		<echo message="Compile Open JUMP core" />
		<delete dir="${temp.dir}" />
		<mkdir dir="${temp.dir}"/>
		<javac	srcdir="../OpenJumpCore/src"
			destdir="${temp.dir}"
			compiler="javac10+"
			source="10" target="10"
			debug="true"
			modulepathref="common-modulepath"
			includeantruntime="false"
		/>
		<jar destfile="${lib.dir}/${jump-core-jar}" includes="**/*.class,**/*.png" basedir="${temp.dir}">
		</jar>
	</target>
	<target name="jump-ui" description="Build OpenJUMP UI jar">
		<echo message="Compile Open JUMP UI" />
		<delete dir="${temp.dir}" />
		<mkdir dir="${temp.dir}"/>
		<javac	srcdir="../OpenJumpUI/src"
		       	destdir="${temp.dir}"
				compiler="javac10+"
				source="10" target="10"
				debug="true"
				modulepathref="common-modulepath"
				includeantruntime="false"
		/>
	    <jar destfile="${lib.dir}/${jump-ui-jar}" includes="**/*.class,**/*.png" basedir="${temp.dir}"/>
	</target>
	
	<!-- Shapefile Viewer -->
	<target name="shape-viewer" description="Build the Shapefile Viewer">
		<echo message="Compile the Shapefile Viewer" />
		<delete dir="${temp.dir}" />
		<mkdir dir="${temp.dir}"/>
		<javac	srcdir="../ShapeViewer/src"
				destdir="${temp.dir}"
				compiler="javac10+"
				source="10" target="10"
				debug="true"
				modulepathref="common-modulepath"
				includeantruntime="false"
		/>
		<jar destfile="${lib.dir}/${shape-viewer-jar}" includes="**/*.class,**/*.png" basedir="${temp.dir}">
			<manifest>
				<attribute name="Main-Class" value="redistrict.colorado.ShapefileViewer"/>
			</manifest>
		</jar>
	</target>
	
	<!-- Copy the new jar file into the local deployment area. -->
	<target name="deploy" >
		<echo message="Copying jar files into ${install.dir}/lib" />
		<mkdir dir="${install.dir}/lib" />
		<copy todir="${install.dir}/lib">
			<fileset dir="${lib.dir}">
				<include name="*.jar"/>
			</fileset>
		</copy>
		<echo message="Copying script files into ${install.dir}/bin" />
		<mkdir dir="${install.dir}/bin" />
		<copy todir="${install.dir}/bin">
			<fileset dir="${basedir}">
				<include name="*.sh"/>
			</fileset>
			</copy>
		<chmod dir="${install.dir}/bin" perm="ugo+rx"
			includes="*.sh"/>
	</target>
</project>